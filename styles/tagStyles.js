import { StyleSheet } from "react-native";

export const tagStyles = StyleSheet.create({
  tag: {
    backgroundColor: "#444",
    color: "#fff",
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 12,
    marginRight: 8,
    marginBottom: 8,
    fontSize: 12,
  },
});
